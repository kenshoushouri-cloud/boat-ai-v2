name: Railway opponent pressure Shadow pilot

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - '.github/scripts/opponent_pressure_shadow_collector.py'
      - '.github/workflows/railway-opponent-pressure-shadow-dry-run.yml'
  issue_comment:
    types: [created]

permissions:
  contents: read

env:
  RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

jobs:
  audit:
    if: >-
      github.event_name == 'pull_request' ||
      (
        github.event_name == 'issue_comment' &&
        github.event.issue.number == 42 &&
        github.actor == github.repository_owner &&
        github.event.comment.body == '/railway opponent-pressure-shadow-pilot'
      )
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'issue_comment' && 'main' || '' }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: |
          set -euo pipefail
          pip install -r requirements.txt
          npm install --global @railway/cli
      - name: Load public database URL without publishing it
        run: |
          set -euo pipefail
          railway variable list --service postgres --environment production --json > railway-vars.json 2>/dev/null
          python - <<'PY'
          import json, os
          from pathlib import Path
          data=json.loads(Path('railway-vars.json').read_text(encoding='utf-8'))
          value=data.get('DATABASE_PUBLIC_URL') if isinstance(data,dict) else None
          if isinstance(data,list):
              for item in data:
                  if isinstance(item,dict) and (item.get('name')=='DATABASE_PUBLIC_URL' or item.get('key')=='DATABASE_PUBLIC_URL'):
                      value=item.get('value'); break
          if not value: raise SystemExit('DATABASE_PUBLIC_URL not found')
          print(f'::add-mask::{value}')
          with open(os.environ['GITHUB_ENV'],'a',encoding='utf-8') as fh: fh.write(f'DATABASE_URL={value}\n')
          PY
      - name: Run Shadow collector
        id: audit
        env:
          OPPONENT_PRESSURE_SHADOW_ENABLED: '1'
          OPPONENT_PRESSURE_SHADOW_DRY_RUN: ${{ github.event_name == 'pull_request' && '1' || '0' }}
          TARGET_DATE: '2026-08-22'
          WINDOW_NAME: ${{ github.event_name == 'pull_request' && 'ci_dry_run' || 'pilot_20260822' }}
        run: |
          set +e
          python -u .github/scripts/opponent_pressure_shadow_collector.py > pressure-shadow-output.txt 2>&1
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Print sanitized results
        if: always()
        run: |
          python - <<'PY'
          from pathlib import Path
          text=Path('pressure-shadow-output.txt').read_text(encoding='utf-8',errors='replace') if Path('pressure-shadow-output.txt').exists() else ''
          for line in text.splitlines():
              if line.startswith('OPP_PRESSURE_SHADOW_'):
                  print(line[:1000])
          PY
      - name: Verify one-day pilot postconditions
        if: github.event_name == 'issue_comment' && steps.audit.outputs.exit_code == '0'
        run: |
          python - <<'PY'
          import os, psycopg
          from psycopg.rows import dict_row
          with psycopg.connect(os.environ['DATABASE_URL'],row_factory=dict_row) as conn:
              with conn.cursor() as cur:
                  cur.execute("""
                    select count(*) total,
                           count(*) filter(where race_date=date '2026-08-22') target_rows,
                           count(*) filter(where race_date<>date '2026-08-22') non_target_rows,
                           count(*) filter(where jsonb_array_length(lane_scores)=6) six_lane_rows
                    from v2_opponent_pressure_shadow
                  """)
                  r=cur.fetchone()
          print(f"OPP_PRESSURE_SHADOW_POST=total:{r['total']} target:{r['target_rows']} non_target:{r['non_target_rows']} six_lane:{r['six_lane_rows']}")
          if r['target_rows'] != 156 or r['non_target_rows'] != 0 or r['six_lane_rows'] != r['target_rows']:
              raise SystemExit('pilot postcondition failed')
          print('OPP_PRESSURE_SHADOW_POST_RESULT=PASS')
          PY
      - name: Clean temporary files
        if: always()
        run: rm -f railway-vars.json pressure-shadow-output.txt
      - name: Fail if collector failed
        if: steps.audit.outputs.exit_code != '0'
        run: exit 1
